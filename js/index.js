const swiper = new Swiper('.swiper-reviews', {
    // Optional parameters
    slidesPerView: 3.3,
    centeredSlides: true,
    spaceBetween: 50,
    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  
  });

